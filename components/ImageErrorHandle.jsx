import { ErrorBoundary } from 'next/dist/client/components/error-boundary';
import React from 'react';

const ImageErrorHandle = ({imageSrc}) => {
    return (
        
    );
};

export default ImageErrorHandle;